/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["m.media-amazon.com", "encrypted-tbn0.gstatic.com", "i.ytimg.com", "bisuteriacoketa.wordpress.com"],
  }
};

export default nextConfig;
